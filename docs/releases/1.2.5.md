# AERIS 1.2.5

The gadget now focuses on four stacked-bar sections: CPU, memory, network and storage.

- Remove duplicate CPU history and ranked app lists from the full gadget; keep details on hover.
- Retain stable app colors for CPU and memory. Memory keeps the physical used/free boundary, with process proportions inside its used area.
- Add receive/send network proportions with live rates.
- Add per-volume used/free storage bars and read/write rates. Additional volumes are scrollable.
- Reduce the full gadget to 340 × 540; preserve the 560 × 76 mini-bar and update screenshots.

CPU composition uses measured process totals. Memory colors are not physical RAM attribution. Network proportions are not link-capacity utilization or per-process traffic.

Windows x64 installer/portable ZIP, macOS Apple Silicon/Intel DMGs and Linux x64 DEB/AppImage are built and tested by GitHub Actions. Each package includes a SHA-256 sidecar. Windows builds are unsigned; macOS builds use ad-hoc signing without notarization. The interface is Japanese.
