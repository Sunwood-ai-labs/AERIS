{
  "name": "aeris-task-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 1420 --strictPort",
    "build": "tsc --noEmit && vite build",
    "test": "node --import tsx --test tests/*.test.ts",
    "tauri": "tauri",
    "desktop": "tauri dev",
    "release": "tauri build --no-bundle"
  },
  "dependencies": { "@tauri-apps/api": "^2.10.1", "lucide": "^0.577.0" },
  "devDependencies": { "@tauri-apps/cli": "^2.10.1", "vite": "^7.3.1", "typescript": "^5.9.3", "tsx": "^4.21.0", "@types/node": "^24.0.0" }
}
